# 快速开始

<cite>
**本文引用的文件**
- [README.md](file://service/README.md)
- [README.md](file://README.md)
- [pyproject.toml](file://service/pyproject.toml)
- [main.py](file://service/src/main.py)
- [settings.py](file://service/src/config/settings.py)
- [cli.py](file://service/scripts/cli.py)
- [setup_dev.sh](file://service/scripts/setup_dev.sh)
- [setup_dev.bat](file://service/scripts/setup_dev.bat)
- [Dockerfile](file://service/docker/Dockerfile)
- [docker-compose.yml](file://service/docker/docker-compose.yml)
- [.env.example](file://service/.env.example)
- [system_routes.py](file://service/src/api/v1/system_routes.py)
- [constants.py](file://service/src/core/constants.py)
</cite>

## 更新摘要
**所做更改**
- 更新了项目概述和架构设计，反映完整的README.md主文档
- 新增了详细的环境要求说明，包括Python 3.10+和UV包管理工具
- 完善了安装步骤，涵盖Linux/Mac和Windows平台的不同安装方法
- 详细说明了手动安装流程，从虚拟环境创建到依赖安装
- 提供了启动服务的具体命令和访问说明
- 增强了环境配置指导，包括多环境配置管理和环境变量设置
- 新增了API接口概览和部署指南

## 目录
1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [快速开始](#快速开始)
5. [环境要求](#环境要求)
6. [安装步骤](#安装步骤)
7. [手动安装流程](#手动安装流程)
8. [启动服务](#启动服务)
9. [环境配置](#环境配置)
10. [管理命令](#管理命令)
11. [API接口概览](#api接口概览)
12. [部署指南](#部署指南)
13. [故障排除](#故障排除)

## 项目概述

Hello-FastApi是一个基于FastAPI框架的RESTful API服务，采用DDD（领域驱动设计）架构和RBAC权限控制。该项目是一个高度模块化、可扩展的API服务系统，具有以下特性：

- **DDD架构**：清晰的分层设计，业务领域与技术实现分离
- **RBAC权限**：基于角色的访问控制，支持精细化权限管理
- **JWT认证**：现代化的Token认证机制
- **异步处理**：全面支持async/await，高性能并发处理
- **类型安全**：完整的类型提示，Pydantic数据验证

## 技术栈

### 后端技术栈

| 类别 | 技术 |
|------|------|
| 框架 | FastAPI |
| ORM | SQLAlchemy 2.0+ (AsyncIO) |
| 数据库 | SQLite (开发) / PostgreSQL (生产) |
| 缓存 | Redis |
| 认证 | JWT (python-jose) |
| 验证 | Pydantic |
| 日志 | Loguru |

### 前端技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Vue3 + Vite + Element-Plus + TypeScript + Pinia + TailwindCSS |
| 构建工具 | Vite 8+ |
| 状态管理 | Pinia |
| CSS框架 | TailwindCSS 4 |
| HTTP客户端 | Axios |
| 国际化 | vue-i18n |
| 数据可视化 | ECharts 6 |

## 项目结构

```
Hello-FastApi/
├── service/                    # 后端服务（FastAPI）
│   ├── src/
│   │   ├── api/v1/            # API路由层
│   │   ├── application/       # 应用层（DTO + Service）
│   │   ├── config/            # 配置管理
│   │   ├── core/              # 核心模块（中间件、异常、日志）
│   │   ├── domain/            # 领域层（实体模型）
│   │   ├── infrastructure/    # 基础设施层（数据库、缓存、仓储）
│   │   └── main.py            # 应用入口
│   ├── tests/                 # 测试代码
│   ├── docker/                # Docker配置
│   ├── scripts/               # CLI管理脚本
│   └── pyproject.toml
└── web/                        # 前端（Vue3 Pure Admin）
    ├── src/
    │   ├── api/               # API调用
    │   ├── views/             # 页面组件
    │   ├── router/            # 路由配置
    │   ├── store/             # 状态管理
    │   ├── components/        # 公共组件
    │   ├── utils/             # 工具函数
    │   └── layout/            # 布局组件
    ├── mock/                  # Mock数据
    └── package.json
```

## 快速开始

### 环境要求

- Python >= 3.10
- Node.js >= 20.19 或 >= 22.13
- pnpm >= 9
- Redis（可选，开发环境可不开启）

### 后端启动

**方式一：使用脚本一键配置（推荐）**

Linux/Mac:
```bash
cd service
bash scripts/setup_dev.sh
```

Windows:
```cmd
cd service
scripts\setup_dev.bat
```

**方式二：手动配置**

```bash
cd service
uv venv --python 3.10
# Windows
.venv\Scripts\activate
# Linux/Mac
source .venv/bin/activate
uv pip install -e ".[dev]"

# 初始化数据库
python -m scripts.cli initdb

# 创建超级管理员
python -m scripts.cli createsuperuser

# 初始化RBAC数据（角色和权限）
python -m scripts.cli seedrbac

# 启动开发服务器
python -m scripts.cli runserver
# API文档：http://localhost:8000/api/system/docs
```

### 前端启动

```bash
cd web
pnpm install
pnpm dev
# 访问地址：http://localhost:8848
```

### 前后端联调

1. 先启动后端服务（端口 8000）
2. 再启动前端服务（端口 8848）
3. 后端CORS已配置允许前端开发地址访问
4. 前端通过axios直接请求后端API（http://localhost:8000/api/system/...）

## 环境要求

### 后端环境要求

- Python >= 3.10
- UV（Python包管理工具）

### 前端环境要求

- Node.js >= 20.19 或 >= 22.13
- pnpm >= 9

## 安装步骤

### Linux/Mac安装

使用自动化脚本（推荐）：
```bash
cd service
bash scripts/setup_dev.sh
```

脚本功能：
- 安装/检测UV
- 创建虚拟环境
- 激活虚拟环境
- 安装开发依赖
- 代码格式化
- 初始化数据库
- 填充RBAC数据
- 运行测试

### Windows安装

使用自动化脚本（推荐）：
```cmd
cd service
scripts\setup_dev.bat
```

功能同上，适用于Windows环境。

## 手动安装流程

### 安装UV

Linux/Mac：
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

Windows：
```powershell
# PowerShell下执行安装脚本
```

### 创建并激活虚拟环境

```bash
# 创建虚拟环境
uv venv --python 3.10

# Linux/Mac激活
source .venv/bin/activate

# Windows激活
.venv\Scripts\activate
```

### 安装开发依赖

```bash
uv pip install -e ".[dev]"
```

### 初始化数据库与RBAC数据

```bash
# 初始化数据库表
python -m scripts.cli initdb

# 初始化默认角色和权限
python -m scripts.cli seedrbac
```

### 运行测试（可选）

```bash
pytest
```

## 启动服务

### 启动开发服务器

```bash
python -m scripts.cli runserver
```

### 访问地址

- API文档: http://localhost:8000/api/system/docs
- ReDoc文档: http://localhost:8000/api/system/redoc
- 健康检查: http://localhost:8000/health

## 环境配置

### 支持的环境

| 环境 | 配置文件 | 说明 |
|------|----------|------|
| development | `.env.development` | 开发环境，DEBUG=true，详细日志 |
| production | `.env.production` | 生产环境，DEBUG=false，精简日志 |
| testing | `.env.testing` | 测试环境，使用测试数据库 |

### 配置加载顺序

```
系统环境变量 > .env.{APP_ENV} > .env > 默认值
```

### 切换环境

**Linux/Mac:**
```bash
export APP_ENV=production
python -m scripts.cli runserver
```

**Windows:**
```cmd
set APP_ENV=production
python -m scripts.cli runserver
```

### 创建本地配置

```bash
# 复制模板创建本地配置
cp .env.example .env

# 编辑配置
vim .env
```

### 核心配置变量

| 变量名 | 默认值 | 说明 |
|-------|-------|------|
| APP_ENV | development | 运行环境 |
| DATABASE_URL | sqlite+aiosqlite:///./sql/dev.db | 数据库连接 |
| REDIS_URL | redis://localhost:6379/0 | Redis连接 |
| JWT_SECRET_KEY | - | JWT密钥（需设置） |
| SECRET_KEY | - | 应用密钥（需设置） |
| CORS_ORIGINS | http://localhost:3000,... | CORS允许源 |

## 管理命令

```bash
python -m scripts.cli runserver       # 启动开发服务器
python -m scripts.cli createsuperuser # 创建超级用户
python -m scripts.cli initdb          # 初始化数据库
python -m scripts.cli seedrbac        # 填充RBAC初始数据
```

## API接口概览

### 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 认证接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/system/auth/login | 用户登录 |
| POST | /api/system/auth/refresh | 刷新令牌 |

### 用户接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/system/users | 获取用户列表 |
| POST | /api/system/users | 创建用户 |
| GET | /api/system/users/{id} | 获取用户详情 |
| PUT | /api/system/users/{id} | 更新用户 |
| DELETE | /api/system/users/{id} | 删除用户 |

### RBAC接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/system/roles | 获取角色列表 |
| POST | /api/system/roles | 创建角色 |
| GET | /api/system/permissions | 获取权限列表 |
| POST | /api/system/permissions | 创建权限 |

### 菜单接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/system/menus | 获取菜单列表 |
| POST | /api/system/menus | 创建菜单 |
| GET | /api/system/menus/tree | 获取菜单树 |
| GET | /api/system/menus/user | 获取用户菜单 |

## 部署指南

### Docker部署

```bash
cd service/docker

# 启动所有服务（FastAPI + PostgreSQL + Redis）
docker-compose up -d

# 初始化数据库和数据
docker-compose exec app python -m scripts.cli initdb
docker-compose exec app python -m scripts.cli createsuperuser
docker-compose exec app python -m scripts.cli seedrbac
```

服务端口：
- API服务：8000
- PostgreSQL：5432
- Redis：6379

### 生产环境部署

使用Gunicorn + Nginx部署：

```bash
gunicorn src.config.asgi:application -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
```

### Docker镜像构建

```bash
# 多阶段构建优化镜像大小
docker build -t hello-fastapi:latest .
```

## 故障排除

### 无法找到UV

- 确认已安装UV并加入PATH
- Linux/Mac可使用安装脚本
- Windows使用PowerShell安装脚本

### 启动失败或端口占用

- 检查HOST/PORT配置
- 确认端口未被占用
- 必要时修改.env中的PORT

### 数据库初始化失败

- 确保数据库服务可用
- 检查DATABASE_URL
- 先执行initdb再启动服务

### 环境变量未生效

- 确认APP_ENV设置正确
- 检查.env.{APP_ENV}与.env的优先级
- 优先使用系统环境变量

### API文档无法访问

- 确认runserver已启动
- 访问/api/system/docs或/api/system/redoc
- 检查CORS_ORIGINS是否包含前端地址

### 前后端联调问题

- 先启动后端服务（端口8000）
- 再启动前端服务（端口8848）
- 后端CORS已配置允许前端开发地址访问
- 前端通过axios直接请求后端API

**章节来源**
- [README.md:1-259](file://service/README.md#L1-L259)
- [README.md:1-250](file://README.md#L1-L250)
- [pyproject.toml:1-76](file://service/pyproject.toml#L1-L76)
- [main.py:1-96](file://service/src/main.py#L1-L96)
- [settings.py:1-198](file://service/src/config/settings.py#L1-L198)
- [cli.py:1-135](file://service/scripts/cli.py#L1-L135)
- [setup_dev.sh:1-47](file://service/scripts/setup_dev.sh#L1-L47)
- [setup_dev.bat:1-44](file://service/scripts/setup_dev.bat#L1-L44)
- [Dockerfile:1-58](file://service/docker/Dockerfile#L1-L58)
- [docker-compose.yml:1-65](file://service/docker/docker-compose.yml#L1-L65)
- [.env.example:1-63](file://service/.env.example#L1-L63)
- [system_routes.py:1-128](file://service/src/api/v1/system_routes.py#L1-L128)
- [constants.py:1-37](file://service/src/core/constants.py#L1-L37)