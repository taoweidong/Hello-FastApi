## 产品概述

前后端功能联调测试，确保所有页面功能正常运行。从数据库初始化开始，清空 SQLite 测试数据库，运行后端自动化测试用例（含 initdb/seedrbac/createsuperuser 流程验证），启动前后端服务，通过浏览器逐页面操作测试所有功能点，发现并修复所有问题，最后验证前端生产打包流程，确保项目可正常发布到生产环境。

## 页面功能点分析与测试清单

### 1. 登录页面 (`/login`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 1.1 | 账号密码登录 | 输入 admin/admin123 正确登录，跳转首页 | P0 |
| 1.2 | 登录验证 | 输入错误密码，提示登录失败 | P0 |
| 1.3 | 验证码验证 | 输入错误验证码，表单校验不通过（非跳过验证码环境时） | P1 |
| 1.4 | 记住密码 | 勾选"记住密码"后关闭浏览器重新打开，账号密码自动填充 | P2 |
| 1.5 | Enter 键登录 | 在登录表单中按 Enter 键触发登录 | P2 |
| 1.6 | 登录后路由跳转 | 登录成功后自动跳转到首页/第一个菜单页面 | P0 |
| 1.7 | 动态路由加载 | 登录后 `get-async-routes` 接口返回菜单树，侧边栏正确渲染 | P0 |
| 1.8 | Token 存储 | 登录后 accessToken/refreshToken 正确存储到 localStorage | P0 |
| 1.9 | 手机号登录 | 切换到手机号登录标签页（stub 功能） | P3 |
| 1.10 | 二维码登录 | 切换到二维码登录标签页（stub 功能） | P3 |
| 1.11 | 注册 | 切换到注册标签页，填写信息提交注册 | P1 |
| 1.12 | 忘记密码 | 切换到忘记密码标签页，修改密码功能 | P1 |
| 1.13 | 主题切换 | 登录页明暗主题切换 | P3 |
| 1.14 | 国际化切换 | 中英文切换 | P3 |

### 2. 用户管理 (`/system/user`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 2.1 | 用户列表 | 页面加载后显示用户分页列表，数据正确 | P0 |
| 2.2 | 按用户名搜索 | 输入用户名点击搜索，列表过滤显示 | P0 |
| 2.3 | 按手机号搜索 | 输入手机号点击搜索，列表过滤显示 | P1 |
| 2.4 | 按状态搜索 | 选择"已开启/已关闭"状态搜索 | P0 |
| 2.5 | 重置搜索 | 点击重置按钮，清空搜索条件，恢复完整列表 | P0 |
| 2.6 | 分页 | 切换页码/每页条数，数据正确加载 | P0 |
| 2.7 | 新增用户 | 点击"新增用户"按钮，弹出表单，填写提交，列表刷新显示新用户 | P0 |
| 2.8 | 修改用户 | 点击行"修改"按钮，弹出编辑表单，修改后保存成功 | P0 |
| 2.9 | 删除用户 | 点击行"删除"按钮，确认后删除，列表刷新 | P0 |
| 2.10 | 批量删除 | 勾选多条记录，点击"批量删除"，确认后删除 | P0 |
| 2.11 | 状态修改（开关） | 点击用户状态开关，切换启用/禁用状态 | P0 |
| 2.12 | 重置密码 | 点击行"更多→重置密码"，输入新密码，重置成功 | P0 |
| 2.13 | 分配角色 | 点击行"更多→分配角色"，弹出角色选择框，分配后保存 | P0 |
| 2.14 | 上传头像 | 点击行"更多→上传头像"，上传图片（stub 功能） | P2 |
| 2.15 | 部门树筛选 | 左侧部门树点击节点，右侧列表按部门过滤 | P1 |
| 2.16 | 取消选择 | 批量操作时点击"取消选择" | P2 |

### 3. 角色管理 (`/system/role`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 3.1 | 角色列表 | 页面加载后显示角色分页列表 | P0 |
| 3.2 | 按角色名搜索 | 输入角色名称搜索 | P0 |
| 3.3 | 按角色标识搜索 | 输入角色标识(code)搜索 | P1 |
| 3.4 | 按状态搜索 | 选择启用/停用状态搜索 | P0 |
| 3.5 | 重置搜索 | 重置搜索条件 | P0 |
| 3.6 | 分页 | 切换页码/每页条数 | P0 |
| 3.7 | 新增角色 | 点击"新增角色"，弹出表单，填写名称/标识/描述，提交成功 | P0 |
| 3.8 | 修改角色 | 点击行"修改"，编辑角色信息，保存成功 | P0 |
| 3.9 | 删除角色 | 点击行"删除"，确认后删除 | P0 |
| 3.10 | 状态修改（开关） | 点击角色状态开关，切换启用/禁用 | P0 |
| 3.11 | 菜单权限分配 | 点击行"权限"按钮，右侧展开菜单权限树 | P0 |
| 3.12 | 权限树加载 | 权限树正确加载所有菜单节点 | P0 |
| 3.13 | 权限树勾选 | 根据角色已分配权限勾选对应节点 | P0 |
| 3.14 | 权限树搜索 | 输入菜单名称过滤权限树 | P1 |
| 3.15 | 展开/折叠 | 勾选"展开/折叠"控制权限树展开状态 | P2 |
| 3.16 | 全选/全不选 | 勾选"全选/全不选" | P2 |
| 3.17 | 父子联动 | 勾选"父子联动"后，勾选父节点自动勾选子节点 | P1 |
| 3.18 | 保存权限 | 点击保存按钮，菜单权限保存成功 | P0 |
| 3.19 | 关闭权限面板 | 点击关闭按钮收起权限面板 | P2 |

### 4. 菜单管理 (`/system/menu`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 4.1 | 菜单列表 | 页面加载后显示菜单树形表格 | P0 |
| 4.2 | 按菜单名搜索 | 输入菜单名称搜索 | P0 |
| 4.3 | 重置搜索 | 重置搜索条件 | P0 |
| 4.4 | 菜单树展示 | 左侧菜单结构树正确展示层级关系 | P0 |
| 4.5 | 树节点选中 | 点击树节点，右侧表格高亮/过滤 | P1 |
| 4.6 | 新增根菜单 | 点击"新增根菜单"按钮，弹出表单 | P0 |
| 4.7 | 新增子菜单 | 点击行"新增"按钮，创建子菜单 | P0 |
| 4.8 | 修改菜单 | 点击行"修改"按钮，编辑菜单信息 | P0 |
| 4.9 | 删除菜单 | 点击行"删除"按钮，确认删除（有子菜单时提示） | P0 |
| 4.10 | 菜单类型 | 创建目录(0)/页面(1)/权限(2)三种类型 | P0 |
| 4.11 | 菜单元数据 | 创建菜单时填写 meta 信息（标题/图标/是否显示/缓存等） | P0 |
| 4.12 | 上级菜单选择 | 新增/编辑时选择上级菜单 | P1 |
| 4.13 | 排序(rank) | 菜单排序字段正确显示和保存 | P1 |
| 4.14 | 路由路径(path) | 菜单路径字段正确显示和保存 | P0 |
| 4.15 | 组件路径(component) | 页面类型菜单的组件路径正确 | P0 |
| 4.16 | 权限标识(method) | 权限类型菜单的权限标识正确 | P1 |

### 5. 部门管理 (`/system/dept`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 5.1 | 部门列表 | 页面加载后显示部门树形表格（默认展开全部） | P0 |
| 5.2 | 按部门名搜索 | 输入部门名称搜索 | P0 |
| 5.3 | 按状态搜索 | 选择启用/停用状态搜索 | P0 |
| 5.4 | 重置搜索 | 重置搜索条件 | P0 |
| 5.5 | 新增根部门 | 点击"新增部门"按钮 | P0 |
| 5.6 | 新增子部门 | 点击行"新增"按钮，创建子部门 | P0 |
| 5.7 | 修改部门 | 点击行"修改"按钮，编辑部门信息 | P0 |
| 5.8 | 删除部门 | 点击行"删除"按钮，确认删除 | P0 |
| 5.9 | 部门字段 | 部门名称/编码/权限模式/排序/自动绑定/状态/描述 | P1 |
| 5.10 | 树形展开/折叠 | 部门树正确展示层级关系 | P0 |

### 6. 字典管理 (`/system/dictionary`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 6.1 | 字典类型树 | 左侧显示字典类型树 | P0 |
| 6.2 | 字典类型搜索 | 输入字典名称过滤左侧树 | P1 |
| 6.3 | 字典类型选中 | 点击树节点，右侧显示该类型的字典详情列表 | P0 |
| 6.4 | 新增字典类型 | 点击"新增子典"按钮，创建字典类型 | P0 |
| 6.5 | 修改字典类型 | 右键字典类型→修改 | P0 |
| 6.6 | 删除字典类型 | 右键字典类型→删除 | P0 |
| 6.7 | 字典详情列表 | 右侧表格显示字典项列表 | P0 |
| 6.8 | 新增字典详情 | 点击"新增字典详情"按钮 | P0 |
| 6.9 | 修改字典详情 | 点击行"修改"按钮 | P0 |
| 6.10 | 删除字典详情 | 点击行"删除"按钮 | P0 |
| 6.11 | 按名称查询字典 | `getByName` 接口查询字典项 | P1 |

### 7. 权限管理 (`/system/permission`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 7.1 | 角色列表 | 左侧显示角色列表 | P0 |
| 7.2 | 选择角色 | 点击角色行或"配置权限"按钮，右侧显示菜单权限树 | P0 |
| 7.3 | 菜单权限树 | 右侧树正确展示菜单权限层级（目录/页面/权限标签区分） | P0 |
| 7.4 | 勾选权限 | 根据角色已有权限勾选对应节点 | P0 |
| 7.5 | 保存权限 | 点击"保存权限"按钮，保存成功 | P0 |
| 7.6 | 未选角色提示 | 未选择角色时右侧显示"请在左侧选择一个角色"空状态 | P1 |

### 8. IP 规则管理 (`/system/ip-rule`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 8.1 | IP 规则列表 | 页面加载后显示 IP 规则分页列表 | P0 |
| 8.2 | 按规则类型搜索 | 选择黑名单/白名单类型搜索 | P0 |
| 8.3 | 按状态搜索 | 选择启用/停用状态搜索 | P0 |
| 8.4 | 重置搜索 | 重置搜索条件 | P0 |
| 8.5 | 分页 | 切换页码/每页条数 | P0 |
| 8.6 | 新增规则 | 点击"新增规则"按钮，填写 IP/类型/描述，提交成功 | P0 |
| 8.7 | 修改规则 | 点击行"修改"按钮，编辑规则 | P0 |
| 8.8 | 删除规则 | 点击行"删除"按钮，确认删除 | P0 |
| 8.9 | 批量删除 | 点击"批量删除"按钮，删除选中规则 | P0 |
| 8.10 | 清空全部 | 点击"清空全部"按钮，清空所有 IP 规则 | P0 |
| 8.11 | 状态修改（开关） | 点击规则状态开关，切换启用/禁用 | P0 |

### 9. 系统配置 (`/system/config`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 9.1 | 配置列表 | 页面加载后显示配置分页列表 | P0 |
| 9.2 | 按配置键搜索 | 输入配置键搜索 | P0 |
| 9.3 | 重置搜索 | 重置搜索条件 | P0 |
| 9.4 | 分页 | 切换页码/每页条数 | P0 |
| 9.5 | 新增配置 | 点击"新增配置"按钮，填写键/值/描述，提交成功 | P0 |
| 9.6 | 修改配置 | 点击行"修改"按钮，编辑配置 | P0 |
| 9.7 | 删除配置 | 点击行"删除"按钮，确认删除 | P0 |

### 10. 登录日志 (`/monitor/logs/login`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 10.1 | 日志列表 | 页面加载后显示登录日志分页列表 | P0 |
| 10.2 | 按登录类型搜索 | 选择密码/短信/OAuth 类型搜索 | P1 |
| 10.3 | 按登录状态搜索 | 选择成功/失败搜索 | P1 |
| 10.4 | 按时间范围搜索 | 选择日期时间范围搜索 | P1 |
| 10.5 | 重置搜索 | 重置搜索条件 | P0 |
| 10.6 | 分页 | 切换页码/每页条数 | P0 |
| 10.7 | 批量删除 | 勾选多条记录，点击"批量删除" | P0 |
| 10.8 | 清空日志 | 点击"清空日志"按钮，确认后清空所有登录日志 | P0 |
| 10.9 | 取消选择 | 批量操作时点击"取消选择" | P2 |

### 11. 操作日志 (`/monitor/logs/operation`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 11.1 | 日志列表 | 页面加载后显示操作日志分页列表 | P0 |
| 11.2 | 按模块搜索 | 输入所属模块搜索 | P1 |
| 11.3 | 按操作状态搜索 | 选择成功/失败搜索 | P1 |
| 11.4 | 按时间范围搜索 | 选择日期时间范围搜索 | P1 |
| 11.5 | 重置搜索 | 重置搜索条件 | P0 |
| 11.6 | 分页 | 切换页码/每页条数 | P0 |
| 11.7 | 批量删除 | 勾选多条记录，点击"批量删除" | P0 |
| 11.8 | 清空日志 | 点击"清空日志"按钮，确认后清空 | P0 |
| 11.9 | 取消选择 | 批量操作时点击"取消选择" | P2 |

### 12. 系统日志 (`/monitor/logs/system`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 12.1 | 日志列表 | 页面加载后显示系统日志分页列表 | P0 |
| 12.2 | 按模块搜索 | 输入所属模块搜索 | P1 |
| 12.3 | 按时间范围搜索 | 选择日期时间范围搜索 | P1 |
| 12.4 | 重置搜索 | 重置搜索条件 | P0 |
| 12.5 | 分页 | 切换页码/每页条数 | P0 |
| 12.6 | 查看详情 | 点击行"详情"按钮，查看日志详细信息 | P0 |
| 12.7 | 双击查看详情 | 双击单元格查看详情 | P2 |

### 13. 在线用户 (`/monitor/online`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 13.1 | 在线用户列表 | 页面加载后显示在线用户列表（stub 数据） | P0 |
| 13.2 | 按用户名搜索 | 输入用户名搜索 | P1 |
| 13.3 | 重置搜索 | 重置搜索条件 | P0 |
| 13.4 | 分页 | 切换页码/每页条数 | P0 |
| 13.5 | 强制下线 | 点击行"强退"按钮，确认后强制下线（stub 实现） | P0 |

### 14. 账户设置 (`/account-settings`)

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 14.1 | 个人信息展示 | 页面加载后显示当前用户头像/昵称/用户名 | P0 |
| 14.2 | 个人信息编辑 | 修改昵称/邮箱/电话/简介，保存成功 | P1 |
| 14.3 | 偏好设置 | 切换到偏好设置面板 | P2 |
| 14.4 | 安全日志 | 切换到安全日志面板（stub 数据） | P2 |
| 14.5 | 账户管理 | 切换到账户管理面板（修改密码等） | P1 |
| 14.6 | 侧栏菜单切换 | 点击左侧菜单项切换面板 | P1 |

### 15. 登出功能

| # | 功能点 | 测试点 | 优先级 |
|---|--------|--------|--------|
| 15.1 | 退出登录 | 点击头像下拉菜单中的"退出登录"，返回登录页 | P0 |
| 15.2 | Token 黑名单 | 退出后旧 token 不可用 | P0 |
| 15.3 | Token 刷新 | accessToken 过期后自动使用 refreshToken 刷新 | P0 |
| 15.4 | 401 处理 | token 过期且刷新失败时，跳转到登录页 | P0 |

---

## 核心功能

- **阶段一：数据库初始化验证**
  - 清空 SQLite 测试数据库（删除 `service/sql/test.db`）
  - 执行 `python -m scripts.cli initdb` 验证建表流程
  - 执行 `python -m scripts.cli seedrbac` 验证 RBAC 种子数据初始化
  - 执行 `python -m scripts.cli seeddata` 验证测试数据初始化
  - 执行 `python -m scripts.cli createsuperuser` 验证超级管理员创建
  - 或一键执行 `python -m scripts.cli initall` 验证完整初始化链路
  - 确认数据库表结构和约束正确（外键、唯一索引等）
  - 确认种子数据完整性（菜单树、角色-菜单关联、用户-角色关联）

- **阶段二：后端自动化测试**
  - 运行 `pytest tests/` 全部测试，确保所有单元测试和集成测试通过
  - 单元测试使用内存 SQLite（conftest.py 自动创建/销毁表）
  - 集成测试使用共享内存 SQLite + db_seed 清空再种子数据
  - 重点关注集成测试中覆盖的 API 端点：认证、用户CRUD、角色菜单部门、日志
  - 检查 git 未提交的 IP 规则相关改动是否影响现有功能

- **阶段三：前端 UI 测试（使用 Playwright MCP）**
  - 使用 `[mcp:Playwright MCP Server]` 进行浏览器自动化测试
  - 逐页面访问，验证每个功能按钮和表单操作
  - 按优先级测试：登录 → 用户管理 → 角色管理 → 部门管理 → 菜单管理 → 字典管理 → 权限管理 → IP规则 → 系统配置 → 登录日志 → 操作日志 → 系统日志 → 在线用户 → 账户设置 → 登出

- **阶段四：问题修复**
  - 根据测试结果分类：前端bug / 后端bug / 前后端对接bug
  - 优先修复阻断性问题（页面无法加载、API 500 错误）
  - 然后修复功能性问题（数据不显示、操作不生效）
  - 最后处理体验性问题（提示不友好、样式异常）

- **阶段五：前端生产打包验证**
  - 执行 `pnpm build` 验证生产环境打包是否成功
  - 检查打包产物完整性（dist 目录结构、静态资源、入口文件）
  - 执行 `pnpm typecheck` 验证 TypeScript 类型检查通过
  - 执行 `pnpm lint` 验证代码规范检查通过
  - 使用 `pnpm preview` 启动生产预览服务，验证打包产物可正常运行
  - 验证生产环境下 API 代理配置正确
  - 验证环境变量和平台配置文件（`public/platform-config.json`）在生产包中正确
  - 验证路由模式（history/hash）在生产环境下正常工作
  - 检查打包体积是否合理，无异常大的 chunk
  - 确认 WASM/Worker 等特殊资源（`public/wasm/`）正确复制到 dist
  - 确认字体、图标等静态资源正确打包（无 404 或加载失败）
  - 确认所有动态路由和权限控制在生产包中正常工作

## 技术栈

- 后端: FastAPI + SQLModel + SQLAlchemy 2.0 (AsyncIO) + pytest
- 前端: Vue 3 + TypeScript + Element Plus + Pure Admin
- 测试工具: pytest (后端), Playwright MCP (前端UI测试)
- HTTP客户端: Axios
- 数据库: SQLite (测试环境) / MySQL (开发环境)

## 实施方案

### 方案概述

分五个阶段推进：1) 数据库初始化验证 → 确保建表和种子数据正确；2) 后端自动化测试 → 修复后端问题；3) 前端服务启动+逐页面UI测试 → 记录问题；4) 集中修复所有前后端联调问题；5) 前端生产打包验证 → 确保项目可正常发布到生产环境。

### 测试策略

**阶段一：数据库初始化验证（从零开始）**

关键步骤：
1. 删除旧测试数据库 `service/sql/test.db`（如果存在）
2. 设置 `APP_ENV=testing` 使用 SQLite
3. 执行 `python -m scripts.cli initall` 一键初始化，验证：
   - 建表成功（检查 SQLModel.metadata.create_all 无报错）
   - RBAC 种子数据完整（DEFAULT_MENUS/DEFAULT_ROLES 正确写入）
   - admin 角色分配了所有菜单权限
   - user 角色分配了只读菜单权限
   - 超级管理员创建成功（username=admin, 自动分配 admin 角色）
4. 验证数据库表结构和约束：
   - `sys_users` 表存在，admin 用户 is_superuser=1
   - `sys_roles` 表存在，admin 和 user 角色已创建
   - `sys_menus` + `sys_menumeta` 表存在，菜单树完整
   - `sys_userinfo_roles` 关联正确
   - `sys_userrole_menu` 关联正确（admin 角色有所有菜单）
   - `sys_departments` 表存在
   - `sys_ip_rules` 表存在
   - `sys_systemconfig` 表存在
   - `sys_loginlog` 表存在（种子日志数据）
   - `sys_logs` 表存在（种子系统日志数据）
5. 如初始化过程有报错，定位并修复后再重试

**阶段二：后端测试**

- 运行 `pytest tests/ -v` 全部测试
- 单元测试（7个文件）：test_auth, test_core, test_dto_validators, test_menu_service, test_role_service, test_system_config_service, test_user_service, test_validators
- 集成测试（2个文件）：
  - test_api.py — 基础健康检查/认证/用户端点
  - test_api_real_flow.py — 全链路真实流程（认证/RBAC/用户CRUD/角色菜单部门/日志/桩路由）
- 集成测试每个用例前自动清空表 + 插入种子数据（db_seed.py）
- 检查 IP 规则相关改动（新增 get_effective_ip_rules、缓存方法）是否破坏现有功能

**阶段三：前端 UI 测试（使用 Playwright MCP）**

按以下顺序逐页面测试，P0 优先：

1. **登录页** → 验证登录/登出/Token
2. **用户管理** → 列表/搜索/新增/修改/删除/批量删除/状态/重置密码/分配角色
3. **角色管理** → 列表/搜索/新增/修改/删除/状态/菜单权限分配
4. **部门管理** → 列表/搜索/新增/修改/删除
5. **菜单管理** → 列表/搜索/新增/修改/删除/菜单树
6. **字典管理** → 列表/搜索/新增/修改/删除/字典类型管理
7. **权限管理** → 角色列表/选择角色/权限树/保存权限
8. **IP规则管理** → 列表/搜索/新增/修改/删除/批量删除/清空
9. **系统配置** → 列表/搜索/新增/修改/删除
10. **登录日志** → 列表/搜索/批量删除/清空
11. **操作日志** → 列表/搜索/批量删除/清空
12. **系统日志** → 列表/搜索/查看详情
13. **在线用户** → 列表/搜索/强制下线
14. **账户设置** → 个人信息/偏好/安全日志/账户管理

**阶段四：问题修复**

- 根据测试结果分类：前端bug / 后端bug / 前后端对接bug
- 优先修复阻断性问题（页面无法加载、API 500 错误）
- 然后修复功能性问题（数据不显示、操作不生效）
- 最后处理体验性问题（提示不友好、样式异常）

**阶段五：前端生产打包验证**

- 执行 `pnpm build` 验证生产打包成功，无编译错误和警告
- 执行 `pnpm typecheck` 确保 TypeScript 类型检查通过（零错误）
- 执行 `pnpm lint` 确保代码规范检查通过（零错误）
- 检查 `dist/` 目录结构完整性
- 使用 `pnpm preview` 启动生产预览
- 验证生产环境配置
- 如打包失败或运行异常，定位并修复后重新验证

### 关键技术决策

- 从数据库初始化开始验证，确保不遗漏任何建表/种子数据场景
- 后端测试优先于前端测试，确保 API 正确后再进行 UI 测试
- 使用 Playwright MCP 进行 UI 自动化测试，而非纯手动操作，确保测试可复现
- 问题修复采用"发现一批、修复一批、验证一批"的迭代方式
- SQLite 测试数据库在初始化验证后需保留，供前端联调时后端使用
- 联调验证全部通过后，进行前端生产打包验证，确保整个开发到发布流程闭环

## 实施要点

- 后端测试使用 SQLite 内存数据库（conftest.py 自动管理），不依赖外部服务
- 前端联调需要后端服务运行中，Vite 代理 /api/* 到 localhost:8000
- 前端联调后端使用 MySQL（.env.development），需确保 MySQL 服务可用
- 数据库初始化验证使用 SQLite（.env.testing），与后端测试保持一致
- 前端 HTTP 拦截器已实现 401 处理（`isLoggingOut` 标志位），需验证 token 过期场景
- IP 规则 API 前端仍为内联方式调用，需确认功能正常
- 部分监控页面（在线用户、地图、卡片列表）为 stub 数据，功能有限
- `python -m scripts.cli initall` = initdb + seedrbac + seeddata + createsuperuser 四步合一
- 清空测试数据库时直接删除 test.db 文件即可，initdb 会重新创建
- 前端生产打包验证需在所有联调问题修复后进行，确保打包基于无问题代码
- 生产环境 API 代理需通过 nginx 或 API 网关实现，Vite proxy 仅开发环境生效
- 打包验证重点关注：类型检查通过、lint 通过、构建无错误、预览可正常运行

## 目录结构

```
e:/GitHub/Hello-FastApi/
├── service/
│   ├── sql/
│   │   └── test.db                    # SQLite 测试数据库（初始化验证时创建）
│   ├── tests/
│   │   ├── conftest.py                # 测试固件（内存SQLite + 自动建表/删表）
│   │   ├── unit/                      # 单元测试（7个文件）
│   │   └── integration/
│   │       ├── conftest.py            # 集成测试固件（清空+种子数据）
│   │       ├── db_seed.py             # 种子数据生成器（清空表+按序插入）
│   │       ├── test_api.py            # 基础集成测试
│   │       └── test_api_real_flow.py  # 全链路真实流程测试
│   ├── scripts/cli.py                 # CLI工具：initdb/seedrbac/seeddata/initall
│   ├── .env.testing                   # 测试环境（SQLite）
│   ├── .env.development               # 开发环境（MySQL）
│   └── src/
│       ├── api/v1/                    # 10个路由模块
│       ├── application/dto/           # DTO
│       ├── application/services/      # 服务层
│       ├── domain/rbac_defaults.py    # RBAC 默认菜单/角色定义
│       └── infrastructure/
│           ├── database/models/       # SQLModel ORM 模型（sys_前缀表）
│           ├── database/database_manager.py  # 数据库管理器
│           └── http/ip_filter_cache.py # IP规则缓存（新文件）
└── web/
    ├── src/
    │   ├── api/system/                # API 子模块
    │   ├── utils/http/index.ts        # HTTP 拦截器
    │   └── views/                     # 页面组件
    └── vite.config.ts                 # Vite 代理配置
```

## MCP

- **Playwright MCP Server**
- Purpose: 前端 UI 自动化测试，逐页面操作验证所有功能点
- Expected outcome: 覆盖所有页面的增删改查操作，截图记录问题，验证修复结果

## SubAgent

- **code-explorer**
- Purpose: 在测试过程中定位问题代码，搜索相关文件和依赖关系
- Expected outcome: 快速定位 bug 根因，辅助修复决策
